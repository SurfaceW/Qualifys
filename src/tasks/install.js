/**
 * Install the devDependencies of current project
 * it should be ready!
 */

var utils = require('../util');

var installExp = {};

installExp.installLinterRelatedDependency = function () {
}

module.exports = installExp;
